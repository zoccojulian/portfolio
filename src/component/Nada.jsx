import React from 'react';
import { motion } from 'framer-motion';

const Nada = () => {
    return (
        <motion.div style={ { width: '200px', height: ' 200px', backgroundColor: 'var(--color__texto1)', transition: 'all var(--transition_color)' }} >
            
        </motion.div>
    );
}

export default Nada;
